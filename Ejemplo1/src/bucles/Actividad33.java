package bucles;

import java.util.Scanner;

public class Actividad33 {
	public static void main (String[]args) {
		
		//Metemos el Scanner
		Scanner entrada= new Scanner(System.in);
		
		//Declaramos variables
		int nota;
		 System.out.println("Introduce la nota de los alumnos: ");
		nota=entrada.nextInt();

		if (nota >0 || nota <10);
		nota=entrada.nextInt();
		 System.out.println("Introduce otra nota");

		if (nota < 0 || nota > 10);
		
		 System.out.println("Has introducido una nota no válida. Fin del programa :)))");
		 entrada.close();
		}
	}

